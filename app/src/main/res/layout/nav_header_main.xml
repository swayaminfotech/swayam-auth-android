<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="@dimen/nav_header_height"
    android:background="@color/colorPrimary"
    android:orientation="horizontal"
    android:padding="@dimen/comman_margin"
    android:gravity="center"
    android:theme="@style/ThemeOverlay.AppCompat.Dark">

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="@dimen/side_menu_profile_height"
        android:layout_height="@dimen/side_menu_profile_height"
        android:padding="10dp"
        app:srcCompat="@drawable/profile_placeholder"
        app:tint="@color/white" />

    <com.swayamauth.views.RegularTextView
        android:id="@+id/userName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/appname"
        android:textColor="@color/white"
        android:padding="10dp"
        android:textSize="@dimen/menu_name_text_size"
        android:textAppearance="@style/TextAppearance.AppCompat.Body1" />

</LinearLayout>